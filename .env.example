# ── Konfiguracja Supabase ────────────────────────────────────────────────
# Wartości publiczne (bezpieczne dla klienta) — trafiają też do wrangler.jsonc -> vars.
SUPABASE_URL="https://twoj-projekt.supabase.co"
SUPABASE_ANON_KEY="eyJhbGciOi..."

# ── Sekrety (NIE commitować) ─────────────────────────────────────────────
# Lokalnie trzymane w .dev.vars; na produkcji: `wrangler secret put SUPABASE_SERVICE_ROLE_KEY`.
# Service role omija RLS — używać wyłącznie po stronie serwera w operacjach admina.
# SUPABASE_SERVICE_ROLE_KEY="eyJhbGciOi..."
